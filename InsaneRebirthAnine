local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "RebirthHub"
screenGui.ResetOnSpawn = false
screenGui.Parent = player.PlayerGui

-- MAIN FRAME
local frame = Instance.new("Frame", screenGui)
frame.Size = UDim2.new(0, 240, 0, 200)
frame.Position = UDim2.new(0.5, -120, 0.5, -100)
frame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 14)
local fs = Instance.new("UIStroke", frame)
fs.Color = Color3.fromRGB(120, 60, 255)
fs.Thickness = 2

-- TITLE BAR
local titleBar = Instance.new("Frame", frame)
titleBar.Size = UDim2.new(1, 0, 0, 38)
titleBar.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
titleBar.BorderSizePixel = 0
Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0, 14)
local tbFix = Instance.new("Frame", titleBar)
tbFix.Size = UDim2.new(1, 0, 0.5, 0)
tbFix.Position = UDim2.new(0, 0, 0.5, 0)
tbFix.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
tbFix.BorderSizePixel = 0

local titleLbl = Instance.new("TextLabel", titleBar)
titleLbl.Size = UDim2.new(1, -10, 1, 0)
titleLbl.Position = UDim2.new(0, 12, 0, 0)
titleLbl.BackgroundTransparency = 1
titleLbl.Text = "⚡ RebirthHub"
titleLbl.TextColor3 = Color3.fromRGB(180, 100, 255)
titleLbl.Font = Enum.Font.GothamBold
titleLbl.TextSize = 15
titleLbl.TextXAlignment = Enum.TextXAlignment.Left

-- CONTENT
local layout = Instance.new("UIListLayout", frame)
layout.Padding = UDim.new(0, 8)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
layout.SortOrder = Enum.SortOrder.LayoutOrder
local pad = Instance.new("UIPadding", frame)
pad.PaddingTop = UDim.new(0, 46)
pad.PaddingBottom = UDim.new(0, 10)
pad.PaddingLeft = UDim.new(0, 10)
pad.PaddingRight = UDim.new(0, 10)

-- AMOUNT ROW
local amtRow = Instance.new("Frame", frame)
amtRow.Size = UDim2.new(1, 0, 0, 34)
amtRow.BackgroundColor3 = Color3.fromRGB(25, 25, 38)
amtRow.BorderSizePixel = 0
amtRow.LayoutOrder = 1
Instance.new("UICorner", amtRow).CornerRadius = UDim.new(0, 9)

local amtLabel = Instance.new("TextLabel", amtRow)
amtLabel.Size = UDim2.new(0.55, 0, 1, 0)
amtLabel.Position = UDim2.new(0, 8, 0, 0)
amtLabel.BackgroundTransparency = 1
amtLabel.Text = "Amount per burst:"
amtLabel.TextColor3 = Color3.fromRGB(200, 200, 210)
amtLabel.Font = Enum.Font.Gotham
amtLabel.TextSize = 11
amtLabel.TextXAlignment = Enum.TextXAlignment.Left

local amtBox = Instance.new("TextBox", amtRow)
amtBox.Size = UDim2.new(0.35, 0, 0, 24)
amtBox.Position = UDim2.new(0.62, 0, 0.5, -12)
amtBox.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
amtBox.Text = "100"
amtBox.TextColor3 = Color3.fromRGB(255, 255, 255)
amtBox.Font = Enum.Font.GothamBold
amtBox.TextSize = 12
amtBox.BorderSizePixel = 0
amtBox.ClearTextOnFocus = false
Instance.new("UICorner", amtBox).CornerRadius = UDim.new(0, 6)

-- STATUS
local statusLbl = Instance.new("TextLabel", frame)
statusLbl.Size = UDim2.new(1, 0, 0, 16)
statusLbl.BackgroundTransparency = 1
statusLbl.Text = "Idle"
statusLbl.TextColor3 = Color3.fromRGB(130, 130, 140)
statusLbl.Font = Enum.Font.Gotham
statusLbl.TextSize = 11
statusLbl.LayoutOrder = 2

-- FIRE ONCE BUTTON
local onceBtn = Instance.new("TextButton", frame)
onceBtn.Size = UDim2.new(1, 0, 0, 38)
onceBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 90)
onceBtn.Text = "⚡ Fire Once"
onceBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
onceBtn.Font = Enum.Font.GothamBold
onceBtn.TextSize = 13
onceBtn.BorderSizePixel = 0
onceBtn.LayoutOrder = 3
Instance.new("UICorner", onceBtn).CornerRadius = UDim.new(0, 11)

-- AUTO TOGGLE
local autoBtn = Instance.new("TextButton", frame)
autoBtn.Size = UDim2.new(1, 0, 0, 38)
autoBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
autoBtn.Text = "🤖 Auto Rebirth: OFF"
autoBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
autoBtn.Font = Enum.Font.GothamBold
autoBtn.TextSize = 13
autoBtn.BorderSizePixel = 0
autoBtn.LayoutOrder = 4
Instance.new("UICorner", autoBtn).CornerRadius = UDim.new(0, 11)

-- LOGIC
local autoRunning = false
local totalFired = 0

local function getAmount()
    return tonumber(amtBox.Text) or 100
end

local function fireRebirth(amount)
    for i = 1, amount do
        task.spawn(function()
            ReplicatedStorage.Events.RequestRebirth:FireServer()
        end)
    end
    totalFired = totalFired + amount
    statusLbl.Text = "✅ Fired " .. amount .. "x | Total: " .. totalFired
    statusLbl.TextColor3 = Color3.fromRGB(100, 220, 100)
end

onceBtn.MouseButton1Click:Connect(function()
    fireRebirth(getAmount())
end)

autoBtn.MouseButton1Click:Connect(function()
    autoRunning = not autoRunning
    if autoRunning then
        autoBtn.Text = "🤖 Auto Rebirth: ON"
        autoBtn.BackgroundColor3 = Color3.fromRGB(120, 60, 255)
        statusLbl.TextColor3 = Color3.fromRGB(180, 100, 255)
        totalFired = 0
        task.spawn(function()
            while autoRunning do
                fireRebirth(getAmount())
                task.wait(0.05)
            end
            statusLbl.Text = "⏹ Stopped | Total fired: " .. totalFired
            statusLbl.TextColor3 = Color3.fromRGB(130, 130, 140)
        end)
    else
        autoRunning = false
        autoBtn.Text = "🤖 Auto Rebirth: OFF"
        autoBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
    end
end)
